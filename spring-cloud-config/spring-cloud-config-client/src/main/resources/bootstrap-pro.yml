spring:
  cloud:
    config:
      name: cloud-config
      profile: pro
#      uri: http://localhost:8001/
#      label: config
      discovery:
        enabled: true
        service-id: spring-cloud-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
